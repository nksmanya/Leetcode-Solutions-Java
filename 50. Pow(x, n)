class Solution {
    public double myPow(double x, int n) {
        double conv = Double.valueOf(n);
        double res = Math.pow(x,conv);
       return res;
    }
}

Link: https://leetcode.com/problems/powx-n/description/
